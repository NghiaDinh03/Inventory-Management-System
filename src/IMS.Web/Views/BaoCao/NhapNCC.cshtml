@using System.Data
@model DataTable
@{
    ViewData["Title"] = "Báo cáo nhập kho theo nhà cung cấp";
}

<!-- Filters section -->
<div class="card-custom mb-4">
    <div class="card-title-custom">
        <span><i class="fa-solid fa-filter text-primary me-2"></i>Lọc dữ liệu nhập kho</span>
    </div>
    <form method="get" action="/BaoCao/NhapNCC" class="row align-items-end g-3">
        <div class="col-md-4">
            <label class="form-label-custom">Chọn nhà cung cấp</label>
            <select name="maNCC" class="form-control-custom w-100" asp-items="ViewBag.NhaCungCaps">
                <option value="">-- Tất cả nhà cung cấp --</option>
            </select>
        </div>
        <div class="col-md-3">
            <label class="form-label-custom">Từ ngày</label>
            <input type="date" name="tuNgay" class="form-control-custom w-100" value="@ViewBag.TuNgay" required />
        </div>
        <div class="col-md-3">
            <label class="form-label-custom">Đến ngày</label>
            <input type="date" name="denNgay" class="form-control-custom w-100" value="@ViewBag.DenNgay" required />
        </div>
        <div class="col-md-2">
            <button type="submit" class="btn-custom btn-primary-custom w-100 py-2 justify-content-center">
                Lọc báo cáo <i class="fa-solid fa-magnifying-glass"></i>
            </button>
        </div>
    </form>
</div>

<!-- Report table list -->
<div class="card-custom">
    <div class="card-title-custom">
        <span>
            <i class="fa-solid fa-list-ol text-primary me-2"></i>
            Chi tiết nhập kho từ ngày <strong>@DateTime.Parse(ViewBag.TuNgay).ToString("dd/MM/yyyy")</strong> đến ngày <strong>@DateTime.Parse(ViewBag.DenNgay).ToString("dd/MM/yyyy")</strong>
        </span>
    </div>

    <div class="table-responsive">
        <table class="table table-custom table-hover datatable-custom">
            <thead>
                <tr>
                    <th>Số phiếu</th>
                    <th>Ngày duyệt</th>
                    <th>Nhà cung cấp</th>
                    <th>Kho hàng</th>
                    <th>Tên sản phẩm</th>
                    <th style="text-align: center;">Số lượng</th>
                    <th style="text-align: right;">Đơn giá</th>
                    <th style="text-align: right;">Thành tiền</th>
                    <th>Người lập</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Rows.Count > 0)
                {
                    foreach (DataRow row in Model.Rows)
                    {
                        var thanhTien = Convert.ToDecimal(row["ThanhTien"]);
                        <tr>
                            <td class="fw-bold text-dark">@row["SoPhieu"]</td>
                            <td>@Convert.ToDateTime(row["NgayDuyet"]).ToString("dd/MM/yyyy HH:mm")</td>
                            <td class="fw-bold text-primary">@row["TenNCC"]</td>
                            <td class="fw-bold text-secondary">@row["TenKho"]</td>
                            <td class="fw-bold text-primary">@row["TenSP"]</td>
                            <td style="text-align: center;" class="fw-bold">@Convert.ToInt32(row["SoLuong"]).ToString("N0")</td>
                            <td class="text-end">@Convert.ToDecimal(row["DonGia"]).ToString("N0") VND</td>
                            <td class="text-end fw-bold text-success">@thanhTien.ToString("N0") VND</td>
                            <td>@row["NguoiLap"]</td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="9" class="text-center text-muted py-4">Không có dữ liệu trong khoảng thời gian đã chọn.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
